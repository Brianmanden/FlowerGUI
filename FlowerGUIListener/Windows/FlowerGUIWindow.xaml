<Window x:Class="FlowerGUIListener.Windows.FlowerGUIWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FlowerGUI" 
        Height="500" Width="800"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual">
    
    <Window.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF66BB6A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF388E3C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FFF44336"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFEF5350"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FFD32F2F"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="White" 
            CornerRadius="10" 
            BorderBrush="#FFCCCCCC" 
            BorderThickness="1"
            Margin="20">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="5" 
                            Direction="315" 
                            Color="Black" 
                            Opacity="0.3" 
                            BlurRadius="20"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Grid Grid.Row="0" Background="#FF2196F3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0">
                    <TextBlock Text="🌸" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="FlowerGUI" 
                             FontSize="16" 
                             FontWeight="Bold" 
                             Foreground="White" 
                             VerticalAlignment="Center"/>
                </StackPanel>
                
                <Button Grid.Column="1" 
                        Style="{StaticResource CloseButtonStyle}"
                        Content="✕" 
                        FontSize="14"
                        Margin="0,0,15,0"
                        Click="CloseButton_Click"/>
            </Grid>
            
            <!-- Content -->
            <ScrollViewer Grid.Row="1" Margin="20" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Velkommen til FlowerGUI!" 
                             FontSize="24" 
                             FontWeight="Bold" 
                             Margin="0,0,0,20" 
                             HorizontalAlignment="Center"/>
                    
                    <TextBlock Text="Aktiveret ved cursor position:" 
                             FontSize="14" 
                             Margin="0,0,0,10"/>
                    
                    <TextBlock x:Name="CursorPositionText" 
                             Text="X: 0, Y: 0" 
                             FontSize="12" 
                             Foreground="#FF666666" 
                             Margin="0,0,0,20"/>
                    
                    <TextBlock Text="Hurtige handlinger:" 
                             FontSize="16" 
                             FontWeight="SemiBold" 
                             Margin="0,0,0,15"/>
                    
                    <WrapPanel HorizontalAlignment="Center">
                        <Button Content="📝 Notat" Style="{StaticResource ModernButtonStyle}" Click="TakeNote_Click"/>
                        <Button Content="📷 Skærmbillede" Style="{StaticResource ModernButtonStyle}" Click="TakeScreenshot_Click"/>
                        <Button Content="📋 Udklipsholder" Style="{StaticResource ModernButtonStyle}" Click="OpenClipboard_Click"/>
                        <Button Content="🔍 Søg" Style="{StaticResource ModernButtonStyle}" Click="Search_Click"/>
                    </WrapPanel>
                    
                    <TextBlock Text="Seneste handlinger:" 
                             FontSize="16" 
                             FontWeight="SemiBold" 
                             Margin="0,20,0,15"/>
                    
                    <ListBox x:Name="RecentActionsListBox" 
                           Height="150" 
                           BorderBrush="#FFDDDDDD" 
                           BorderThickness="1">
                    </ListBox>
                </StackPanel>
            </ScrollViewer>
            
            <!-- Footer -->
            <Grid Grid.Row="2" Background="#FFF5F5F5">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="Tryk Esc for at lukke | Ctrl+H for hjælp" 
                             FontSize="12" 
                             Foreground="#FF666666"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
